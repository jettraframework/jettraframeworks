# Map Example

The `Map` widget is part of the JettraEspressoUI framework.

## Usage

```java
import io.jettra.espresso.widgets.Map;
import io.jettra.espresso.widgets.Text;

Map myMap = Map.of(
    Text.of("This is a Map widget.")
);
```
