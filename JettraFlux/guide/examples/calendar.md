# Calendar Example

The `Calendar` widget is part of the JettraEspressoUI framework.

## Usage

```java
import io.jettra.espresso.widgets.Calendar;
import io.jettra.espresso.widgets.Text;

Calendar myCalendar = Calendar.of(
    Text.of("This is a Calendar widget.")
);
```
