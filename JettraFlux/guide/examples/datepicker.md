# DatePicker Example

The `DatePicker` widget is part of the JettraEspressoUI framework.

## Usage

```java
import io.jettra.espresso.widgets.DatePicker;
import io.jettra.espresso.widgets.Text;

DatePicker myDatePicker = DatePicker.of(
    Text.of("This is a DatePicker widget.")
);
```
