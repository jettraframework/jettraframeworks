# QR Example

The `QR` widget is part of the JettraEspressoUI framework.

## Usage

```java
import io.jettra.espresso.widgets.QR;
import io.jettra.espresso.widgets.Text;

QR myQR = QR.of(
    Text.of("This is a QR widget.")
);
```
