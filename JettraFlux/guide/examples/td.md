# TD Example

The `TD` widget is part of the JettraEspressoUI framework.

## Usage

```java
import io.jettra.espresso.widgets.TD;
import io.jettra.espresso.widgets.Text;

TD myTD = TD.of(
    Text.of("This is a TD widget.")
);
```
