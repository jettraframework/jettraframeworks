# CrudView Example

The `CrudView` widget is part of the JettraEspressoUI framework.

## Usage

```java
import io.jettra.espresso.widgets.CrudView;
import io.jettra.espresso.widgets.Text;

CrudView myCrudView = CrudView.of(
    Text.of("This is a CrudView widget.")
);
```
