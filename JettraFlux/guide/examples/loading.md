# Loading Example

The `Loading` widget is part of the JettraEspressoUI framework.

## Usage

```java
import io.jettra.espresso.widgets.Loading;
import io.jettra.espresso.widgets.Text;

Loading myLoading = Loading.of(
    Text.of("This is a Loading widget.")
);
```
