# Divide Example

The `Divide` widget is part of the JettraEspressoUI framework.

## Usage

```java
import io.jettra.espresso.widgets.Divide;
import io.jettra.espresso.widgets.Text;

Divide myDivide = Divide.of(
    Text.of("This is a Divide widget.")
);
```
