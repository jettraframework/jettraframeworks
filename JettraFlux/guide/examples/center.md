# Center Example

The `Center` widget aligns its single child to the center of its own bounds.

## Usage

```java
import io.jettra.espresso.widgets.Center;
import io.jettra.espresso.widgets.Text;

Center centeredText = Center.of(
    Text.of("I am centered!")
);
```
