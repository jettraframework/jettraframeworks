# Image Example

The `Image` widget renders an `<img>` tag for displaying pictures.

## Usage

```java
import io.jettra.espresso.widgets.Image;

Image logo = Image.of("/assets/logo.png").alt("Company Logo");
```
