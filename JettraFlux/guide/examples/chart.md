# Chart Example

The `Chart` widget is part of the JettraEspressoUI framework.

## Usage

```java
import io.jettra.espresso.widgets.Chart;
import io.jettra.espresso.widgets.Text;

Chart myChart = Chart.of(
    Text.of("This is a Chart widget.")
);
```
