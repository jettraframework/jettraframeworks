# ChartsLine Example

The `ChartsLine` widget is part of the JettraEspressoUI framework.

## Usage

```java
import io.jettra.espresso.widgets.ChartsLine;
import io.jettra.espresso.widgets.Text;

ChartsLine myChartsLine = ChartsLine.of(
    Text.of("This is a ChartsLine widget.")
);
```
