# TabView Example

The `TabView` widget is part of the JettraEspressoUI framework.

## Usage

```java
import io.jettra.espresso.widgets.TabView;
import io.jettra.espresso.widgets.Text;

TabView myTabView = TabView.of(
    Text.of("This is a TabView widget.")
);
```
