# Clock Example

The `Clock` widget is part of the JettraEspressoUI framework.

## Usage

```java
import io.jettra.espresso.widgets.Clock;
import io.jettra.espresso.widgets.Text;

Clock myClock = Clock.of(
    Text.of("This is a Clock widget.")
);
```
