# Panel Example

The `Panel` widget is part of the JettraEspressoUI framework.

## Usage

```java
import io.jettra.espresso.widgets.Panel;
import io.jettra.espresso.widgets.Text;

Panel myPanel = Panel.of(
    Text.of("This is a Panel widget.")
);
```
