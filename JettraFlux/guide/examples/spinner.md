# Spinner Example

The `Spinner` widget is part of the JettraEspressoUI framework.

## Usage

```java
import io.jettra.espresso.widgets.Spinner;
import io.jettra.espresso.widgets.Text;

Spinner mySpinner = Spinner.of(
    Text.of("This is a Spinner widget.")
);
```
