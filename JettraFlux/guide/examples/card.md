# Card Example

The `Card` widget renders a container with a subtle shadow and rounded corners, typically used to display distinct pieces of content.

## Usage

```java
import io.jettra.espresso.widgets.Card;
import io.jettra.espresso.widgets.Text;

Card myCard = Card.of(
    Text.of("This is a card.")
);
```
