# Schedule Example

The `Schedule` widget is part of the JettraEspressoUI framework.

## Usage

```java
import io.jettra.espresso.widgets.Schedule;
import io.jettra.espresso.widgets.Text;

Schedule mySchedule = Schedule.of(
    Text.of("This is a Schedule widget.")
);
```
