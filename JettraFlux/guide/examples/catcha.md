# Catcha Example

The `Catcha` widget is part of the JettraEspressoUI framework.

## Usage

```java
import io.jettra.espresso.widgets.Catcha;
import io.jettra.espresso.widgets.Text;

Catcha myCatcha = Catcha.of(
    Text.of("This is a Catcha widget.")
);
```
