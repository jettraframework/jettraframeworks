# SelectMany Example

The `SelectMany` widget is part of the JettraEspressoUI framework.

## Usage

```java
import io.jettra.espresso.widgets.SelectMany;
import io.jettra.espresso.widgets.Text;

SelectMany mySelectMany = SelectMany.of(
    Text.of("This is a SelectMany widget.")
);
```
