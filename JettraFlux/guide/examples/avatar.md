# Avatar Example

The `Avatar` widget is part of the JettraEspressoUI framework.

## Usage

```java
import io.jettra.espresso.widgets.Avatar;
import io.jettra.espresso.widgets.Text;

Avatar myAvatar = Avatar.of(
    Text.of("This is a Avatar widget.")
);
```
