# ViewMedia Example

The `ViewMedia` widget is part of the JettraEspressoUI framework.

## Usage

```java
import io.jettra.espresso.widgets.ViewMedia;
import io.jettra.espresso.widgets.Text;

ViewMedia myViewMedia = ViewMedia.of(
    Text.of("This is a ViewMedia widget.")
);
```
