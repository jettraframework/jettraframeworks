# AvatarGroup Example

The `AvatarGroup` widget is part of the JettraEspressoUI framework.

## Usage

```java
import io.jettra.espresso.widgets.AvatarGroup;
import io.jettra.espresso.widgets.Text;

AvatarGroup myAvatarGroup = AvatarGroup.of(
    Text.of("This is a AvatarGroup widget.")
);
```
