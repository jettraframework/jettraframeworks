# Organigram Example

The `Organigram` widget is part of the JettraEspressoUI framework.

## Usage

```java
import io.jettra.espresso.widgets.Organigram;
import io.jettra.espresso.widgets.Text;

Organigram myOrganigram = Organigram.of(
    Text.of("This is a Organigram widget.")
);
```
