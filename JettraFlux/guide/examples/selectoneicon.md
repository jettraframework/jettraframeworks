# SelectOneIcon Example

The `SelectOneIcon` widget is part of the JettraEspressoUI framework.

## Usage

```java
import io.jettra.espresso.widgets.SelectOneIcon;
import io.jettra.espresso.widgets.Text;

SelectOneIcon mySelectOneIcon = SelectOneIcon.of(
    Text.of("This is a SelectOneIcon widget.")
);
```
