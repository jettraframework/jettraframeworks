# LoginAdvanced Example

The `LoginAdvanced` widget is part of the JettraEspressoUI framework.

## Usage

```java
import io.jettra.espresso.widgets.LoginAdvanced;
import io.jettra.espresso.widgets.Text;

LoginAdvanced myLoginAdvanced = LoginAdvanced.of(
    Text.of("This is a LoginAdvanced widget.")
);
```
