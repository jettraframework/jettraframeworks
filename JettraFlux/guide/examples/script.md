# Script Example

The `Script` widget is part of the JettraEspressoUI framework.

## Usage

```java
import io.jettra.espresso.widgets.Script;
import io.jettra.espresso.widgets.Text;

Script myScript = Script.of(
    Text.of("This is a Script widget.")
);
```
