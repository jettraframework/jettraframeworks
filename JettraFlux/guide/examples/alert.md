# Alert Example

The `Alert` widget is part of the JettraEspressoUI framework.

## Usage

```java
import io.jettra.espresso.widgets.Alert;
import io.jettra.espresso.widgets.Text;

Alert myAlert = Alert.of(
    Text.of("This is a Alert widget.")
);
```
