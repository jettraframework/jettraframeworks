# Style Example

The `Style` widget is part of the JettraEspressoUI framework.

## Usage

```java
import io.jettra.espresso.widgets.Style;
import io.jettra.espresso.widgets.Text;

Style myStyle = Style.of(
    Text.of("This is a Style widget.")
);
```
