# CreditCard Example

The `CreditCard` widget is part of the JettraEspressoUI framework.

## Usage

```java
import io.jettra.espresso.widgets.CreditCard;
import io.jettra.espresso.widgets.Text;

CreditCard myCreditCard = CreditCard.of(
    Text.of("This is a CreditCard widget.")
);
```
