# Draw Example

The `Draw` widget is part of the JettraEspressoUI framework.

## Usage

```java
import io.jettra.espresso.widgets.Draw;
import io.jettra.espresso.widgets.Text;

Draw myDraw = Draw.of(
    Text.of("This is a Draw widget.")
);
```
