# OTPValidator Example

The `OTPValidator` widget is part of the JettraEspressoUI framework.

## Usage

```java
import io.jettra.espresso.widgets.OTPValidator;
import io.jettra.espresso.widgets.Text;

OTPValidator myOTPValidator = OTPValidator.of(
    Text.of("This is a OTPValidator widget.")
);
```
