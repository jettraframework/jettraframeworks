# FormGroup Example

The `FormGroup` widget is part of the JettraEspressoUI framework.

## Usage

```java
import io.jettra.espresso.widgets.FormGroup;
import io.jettra.espresso.widgets.Text;

FormGroup myFormGroup = FormGroup.of(
    Text.of("This is a FormGroup widget.")
);
```
