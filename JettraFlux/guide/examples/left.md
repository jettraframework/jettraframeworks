# Left Example

The `Left` widget is part of the JettraEspressoUI framework.

## Usage

```java
import io.jettra.espresso.widgets.Left;
import io.jettra.espresso.widgets.Text;

Left myLeft = Left.of(
    Text.of("This is a Left widget.")
);
```
