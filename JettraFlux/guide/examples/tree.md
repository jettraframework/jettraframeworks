# Tree Example

The `Tree` widget is part of the JettraEspressoUI framework.

## Usage

```java
import io.jettra.espresso.widgets.Tree;
import io.jettra.espresso.widgets.Text;

Tree myTree = Tree.of(
    Text.of("This is a Tree widget.")
);
```
