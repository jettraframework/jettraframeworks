# BarCode Example

The `BarCode` widget is part of the JettraEspressoUI framework.

## Usage

```java
import io.jettra.espresso.widgets.BarCode;
import io.jettra.espresso.widgets.Text;

BarCode myBarCode = BarCode.of(
    Text.of("This is a BarCode widget.")
);
```
