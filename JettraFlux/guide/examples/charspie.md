# CharsPie Example

The `CharsPie` widget is part of the JettraEspressoUI framework.

## Usage

```java
import io.jettra.espresso.widgets.CharsPie;
import io.jettra.espresso.widgets.Text;

CharsPie myCharsPie = CharsPie.of(
    Text.of("This is a CharsPie widget.")
);
```
