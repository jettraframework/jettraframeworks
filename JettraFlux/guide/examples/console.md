# Console Example

The `Console` widget is part of the JettraEspressoUI framework.

## Usage

```java
import io.jettra.espresso.widgets.Console;
import io.jettra.espresso.widgets.Text;

Console myConsole = Console.of(
    Text.of("This is a Console widget.")
);
```
