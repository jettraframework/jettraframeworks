# Time Example

The `Time` widget is part of the JettraEspressoUI framework.

## Usage

```java
import io.jettra.espresso.widgets.Time;
import io.jettra.espresso.widgets.Text;

Time myTime = Time.of(
    Text.of("This is a Time widget.")
);
```
