# CharsRadar Example

The `CharsRadar` widget is part of the JettraEspressoUI framework.

## Usage

```java
import io.jettra.espresso.widgets.CharsRadar;
import io.jettra.espresso.widgets.Text;

CharsRadar myCharsRadar = CharsRadar.of(
    Text.of("This is a CharsRadar widget.")
);
```
