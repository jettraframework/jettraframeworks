# Menu Example

The `Menu` widget is part of the JettraEspressoUI framework.

## Usage

```java
import io.jettra.espresso.widgets.Menu;
import io.jettra.espresso.widgets.Text;

Menu myMenu = Menu.of(
    Text.of("This is a Menu widget.")
);
```
