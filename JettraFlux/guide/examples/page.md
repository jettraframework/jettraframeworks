# Page Example

The `Page` widget is part of the JettraEspressoUI framework.

## Usage

```java
import io.jettra.espresso.widgets.Page;
import io.jettra.espresso.widgets.Text;

Page myPage = Page.of(
    Text.of("This is a Page widget.")
);
```
