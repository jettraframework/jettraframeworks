# RadioGroupButton Example

The `RadioGroupButton` widget is part of the JettraEspressoUI framework.

## Usage

```java
import io.jettra.espresso.widgets.RadioGroupButton;
import io.jettra.espresso.widgets.Text;

RadioGroupButton myRadioGroupButton = RadioGroupButton.of(
    Text.of("This is a RadioGroupButton widget.")
);
```
