# Carrousel Example

The `Carrousel` widget is part of the JettraEspressoUI framework.

## Usage

```java
import io.jettra.espresso.widgets.Carrousel;
import io.jettra.espresso.widgets.Text;

Carrousel myCarrousel = Carrousel.of(
    Text.of("This is a Carrousel widget.")
);
```
