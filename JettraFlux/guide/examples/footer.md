# Footer Example

The `Footer` widget is part of the JettraEspressoUI framework.

## Usage

```java
import io.jettra.espresso.widgets.Footer;
import io.jettra.espresso.widgets.Text;

Footer myFooter = Footer.of(
    Text.of("This is a Footer widget.")
);
```
