# LoginUI Example

The `LoginUI` widget is part of the JettraEspressoUI framework.

## Usage

```java
import io.jettra.espresso.widgets.LoginUI;
import io.jettra.espresso.widgets.Text;

LoginUI myLoginUI = LoginUI.of(
    Text.of("This is a LoginUI widget.")
);
```
