# Board Example

The `Board` widget is part of the JettraEspressoUI framework.

## Usage

```java
import io.jettra.espresso.widgets.Board;
import io.jettra.espresso.widgets.Text;

Board myBoard = Board.of(
    Text.of("This is a Board widget.")
);
```
