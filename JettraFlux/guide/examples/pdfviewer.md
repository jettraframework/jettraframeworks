# PDFViewer Example

The `PDFViewer` widget is part of the JettraEspressoUI framework.

## Usage

```java
import io.jettra.espresso.widgets.PDFViewer;
import io.jettra.espresso.widgets.Text;

PDFViewer myPDFViewer = PDFViewer.of(
    Text.of("This is a PDFViewer widget.")
);
```
