# FileUpload Example

The `FileUpload` widget is part of the JettraEspressoUI framework.

## Usage

```java
import io.jettra.espresso.widgets.FileUpload;
import io.jettra.espresso.widgets.Text;

FileUpload myFileUpload = FileUpload.of(
    Text.of("This is a FileUpload widget.")
);
```
