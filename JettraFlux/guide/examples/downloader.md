# Downloader Example

The `Downloader` widget is part of the JettraEspressoUI framework.

## Usage

```java
import io.jettra.espresso.widgets.Downloader;
import io.jettra.espresso.widgets.Text;

Downloader myDownloader = Downloader.of(
    Text.of("This is a Downloader widget.")
);
```
