# CharsBar Example

The `CharsBar` widget is part of the JettraEspressoUI framework.

## Usage

```java
import io.jettra.espresso.widgets.CharsBar;
import io.jettra.espresso.widgets.Text;

CharsBar myCharsBar = CharsBar.of(
    Text.of("This is a CharsBar widget.")
);
```
