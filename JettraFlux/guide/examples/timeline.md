# TimeLine Example

The `TimeLine` widget is part of the JettraEspressoUI framework.

## Usage

```java
import io.jettra.espresso.widgets.TimeLine;
import io.jettra.espresso.widgets.Text;

TimeLine myTimeLine = TimeLine.of(
    Text.of("This is a TimeLine widget.")
);
```
