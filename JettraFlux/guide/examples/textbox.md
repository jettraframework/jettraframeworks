# TextBox Example

The `TextBox` widget is part of the JettraEspressoUI framework.

## Usage

```java
import io.jettra.espresso.widgets.TextBox;
import io.jettra.espresso.widgets.Text;

TextBox myTextBox = TextBox.of(
    Text.of("This is a TextBox widget.")
);
```
