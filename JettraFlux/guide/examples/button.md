# Button Example

The `Button` widget is part of the JettraEspressoUI framework.

## Usage

```java
import io.jettra.espresso.widgets.Button;
import io.jettra.espresso.widgets.Text;

Button myButton = Button.of(
    Text.of("This is a Button widget.")
);
```
