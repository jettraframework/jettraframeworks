# CheckBoxGroup Example

The `CheckBoxGroup` widget is part of the JettraEspressoUI framework.

## Usage

```java
import io.jettra.espresso.widgets.CheckBoxGroup;
import io.jettra.espresso.widgets.Text;

CheckBoxGroup myCheckBoxGroup = CheckBoxGroup.of(
    Text.of("This is a CheckBoxGroup widget.")
);
```
