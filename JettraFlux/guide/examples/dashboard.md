# Dashboard Example

The `Dashboard` widget is part of the JettraEspressoUI framework.

## Usage

```java
import io.jettra.espresso.widgets.Dashboard;
import io.jettra.espresso.widgets.Text;

Dashboard myDashboard = Dashboard.of(
    Text.of("This is a Dashboard widget.")
);
```
