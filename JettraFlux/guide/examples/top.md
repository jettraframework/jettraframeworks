# Top Example

The `Top` widget is part of the JettraEspressoUI framework.

## Usage

```java
import io.jettra.espresso.widgets.Top;
import io.jettra.espresso.widgets.Text;

Top myTop = Top.of(
    Text.of("This is a Top widget.")
);
```
