# TimePicker Example

The `TimePicker` widget is part of the JettraEspressoUI framework.

## Usage

```java
import io.jettra.espresso.widgets.TimePicker;
import io.jettra.espresso.widgets.Text;

TimePicker myTimePicker = TimePicker.of(
    Text.of("This is a TimePicker widget.")
);
```
