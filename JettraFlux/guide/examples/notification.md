# Notification Example

The `Notification` widget is part of the JettraEspressoUI framework.

## Usage

```java
import io.jettra.espresso.widgets.Notification;
import io.jettra.espresso.widgets.Text;

Notification myNotification = Notification.of(
    Text.of("This is a Notification widget.")
);
```
