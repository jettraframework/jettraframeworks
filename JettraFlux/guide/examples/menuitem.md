# MenuItem Example

The `MenuItem` widget is part of the JettraEspressoUI framework.

## Usage

```java
import io.jettra.espresso.widgets.MenuItem;
import io.jettra.espresso.widgets.Text;

MenuItem myMenuItem = MenuItem.of(
    Text.of("This is a MenuItem widget.")
);
```
