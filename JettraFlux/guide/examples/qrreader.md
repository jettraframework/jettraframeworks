# QRReader Example

The `QRReader` widget is part of the JettraEspressoUI framework.

## Usage

```java
import io.jettra.espresso.widgets.QRReader;
import io.jettra.espresso.widgets.Text;

QRReader myQRReader = QRReader.of(
    Text.of("This is a QRReader widget.")
);
```
