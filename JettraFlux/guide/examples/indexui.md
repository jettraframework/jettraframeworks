# IndexUI Example

The `IndexUI` widget is part of the JettraEspressoUI framework.

## Usage

```java
import io.jettra.espresso.widgets.IndexUI;
import io.jettra.espresso.widgets.Text;

IndexUI myIndexUI = IndexUI.of(
    Text.of("This is a IndexUI widget.")
);
```
