# Separator Example

The `Separator` widget is part of the JettraEspressoUI framework.

## Usage

```java
import io.jettra.espresso.widgets.Separator;
import io.jettra.espresso.widgets.Text;

Separator mySeparator = Separator.of(
    Text.of("This is a Separator widget.")
);
```
