# Inputs Example

The `Inputs` widget is part of the JettraEspressoUI framework.

## Usage

```java
import io.jettra.espresso.widgets.Inputs;
import io.jettra.espresso.widgets.Text;

Inputs myInputs = Inputs.of(
    Text.of("This is a Inputs widget.")
);
```
