# MenuBar Example

The `MenuBar` widget is part of the JettraEspressoUI framework.

## Usage

```java
import io.jettra.espresso.widgets.MenuBar;
import io.jettra.espresso.widgets.Text;

MenuBar myMenuBar = MenuBar.of(
    Text.of("This is a MenuBar widget.")
);
```
